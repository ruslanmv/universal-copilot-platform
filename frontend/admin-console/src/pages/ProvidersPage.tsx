import React from "react";

export default function ProvidersPage() {
  return (
    <section>
      <h2>Providers</h2>
      <p>Manage provider preferences per tenant (OpenAI, Anthropic, watsonx.ai, Ollama).</p>
      {/* Form to edit provider configs per tenant */}
    </section>
  );
}
