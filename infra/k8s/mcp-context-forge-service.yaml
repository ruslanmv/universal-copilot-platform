apiVersion: v1
kind: Service
metadata:
  name: mcp-context-forge
  labels:
    app: mcp-context-forge
spec:
  type: ClusterIP
  selector:
    app: mcp-context-forge
  ports:
    - name: http
      port: 4444
      targetPort: 4444
